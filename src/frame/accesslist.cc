#include "accesslist.hh"

AccessList::AccessList(std::shared_ptr<Access> head_in, std::shared_ptr<AccessList> tail_in) : head(head_in), tail(tail_in)
{
    // TODO: Implement constructor for accesslist
}